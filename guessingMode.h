#ifndef GUESSINGMODE_H
#define GUESSINGMODE_H

#include <stdlib.h>
#include "akinator.h"

void         guessingMode       (nodeTree_t * tree, FILE * log);
void         addNewNode         (nodeTree_t * currentNode);

#endif
